import { DateRemainingPipe } from './date-remaining.pipe';

describe('DateRemainingPipe', () => {
  it('create an instance', () => {
    const pipe = new DateRemainingPipe();
    expect(pipe).toBeTruthy();
  });
});
